[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    unit: Pure logic tests (no DB, no network)
    api: API endpoint tests (TestClient + test DB)
    integration: Tests requiring external services
addopts = -v --tb=short --strict-markers
